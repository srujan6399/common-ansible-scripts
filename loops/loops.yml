--- # loops playbook example
- hosts: webserver
  user: test
  become: yes
  connection: ssh
  gather_facts: no

  tasks:
    - name: Add list of users
      user: name={{ item }} state=present
      with_items: 
        - user1
        - user2
        - user3